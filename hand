public class Hand {

    //created the hand for the player
    Hand playerHand;

    playerHand  = new Card[];

    //draw two cards1
    Hand.addCard ();

    Hand.addCard ();

    public boolean checkBlackJack() {
        if (value.Hand == 21) {
            //needs the values of the card below from another class
            System.out.println("This hand has " + " and "
                    + ". This hand wins!");
            return true;
        } else {
            return false;
        }
    }

    public boolean checkBust() {
        if (value.Hand > 21) {
            //needs the values of cards from other class
            System.out.println("This hand is over 21. You lose!");
            return true;
        }
        return false;
    }
}
